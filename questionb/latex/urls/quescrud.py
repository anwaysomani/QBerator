from django.conf.urls import url, include
from .. import views

urlpatterns = [
    #url(r'^questions/$', views.question_list, name='question_list'),
    #url(r'^questions/create/$', views.question_create, name='question_create'),

    # Url
]
